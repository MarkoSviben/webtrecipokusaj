<!DOCTYPE html>
<html>
<head>
  <title>Informacije o ulaznici</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1>Detalji ulaznice</h1>
      <a href="/" class="btn btn-secondary">Povratak</a>
    </div>

    <div class="card">
      <div class="card-body">
        <h5 class="card-title">ID: <%= ticket.id %></h5>
        <p class="card-text"><strong>VAT broj:</strong> <%= ticket.vatin %></p>
        <p class="card-text"><strong>Ime:</strong> <%= ticket.firstName %></p>
        <p class="card-text"><strong>Prezime:</strong> <%= ticket.lastName %></p>
        <p class="card-text"><strong>Korisnik:</strong> <%= user.displayName || user.nickname || user.email %></p>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
